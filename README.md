# ParalivesTraitsProject
BepInEx framework mod for Paralives that adds a trait selection panel to character creation, loads traits from JSON, and persists them across saves. Exposes a public API so other mods can read character traits and build gameplay effects on top of them.
